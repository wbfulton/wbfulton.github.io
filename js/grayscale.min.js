/*!
 * Start Bootstrap - Grayscale v5.0.9 (https://startbootstrap.com/template-overviews/grayscale)
 * Copyright 2013-2019 Start Bootstrap
 * Licensed under MIT (https://github.com/BlackrockDigital/startbootstrap-grayscale/blob/master/LICENSE)
 */

!(function($) {
  var t = function() {
      var t = [];
      return (
        $('img').each(function() {
          var a = $(this).data('alt');
          t.push(a);
        }),
        t
      );
    },
    a = t(),
    r = [];
  $.each(a, function(t) {
    (r[t] = new Image()), (r[t].src = a[t]);
  }),
    $('figure').on('click', function() {
      var t = $(this),
        a = t.index(),
        r = t.children('img'),
        i = r.attr('src'),
        n = r.attr('data-alt'),
        c = n.split('.');
      'gif' === c[1]
        ? r.attr('src', r.data('alt')).attr('data-alt', i)
        : r.attr('src', n).attr('data-alt', r.data('alt')),
        t.toggleClass('play');
    });
})(jQuery);

!(function(e) {
  'use strict';
  e('a.js-scroll-trigger[href*="#"]:not([href="#"])').click(function() {
    if (
      location.pathname.replace(/^\//, '') ==
        this.pathname.replace(/^\//, '') &&
      location.hostname == this.hostname
    ) {
      var a = e(this.hash);
      if ((a = a.length ? a : e('[name=' + this.hash.slice(1) + ']')).length)
        return (
          e('html, body').animate(
            { scrollTop: a.offset().top - 70 },
            1e3,
            'easeInOutExpo'
          ),
          !1
        );
    }
  }),
    e('.js-scroll-trigger').click(function() {
      e('.navbar-collapse').collapse('hide');
    }),
    e('body').scrollspy({ target: '#mainNav', offset: 100 });
  function a() {
    100 < e('#mainNav').offset().top
      ? e('#mainNav').addClass('navbar-shrink')
      : e('#mainNav').removeClass('navbar-shrink');
  }
  a(), e(window).scroll(a);
})(jQuery);
